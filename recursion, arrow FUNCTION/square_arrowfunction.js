// using arrow function  Square area Calculation
let side = parseFloat(prompt("Enter side of square:"));
let squareArea = (side) => side * side;

console.log("Area of square: " + squareArea(side));

